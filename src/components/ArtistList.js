import React, { Component } from 'react';
import { Text } from 'react-native';

class ArtistList extends Component {
  static navigationOptions = {
    title: 'Reactify',
  };

  render() {
    return (
      <Text>ArtistList Component</Text>
    );
  }
}

export default ArtistList;
